name: appointnew
publish_to: 'none'
version: 0.1.0
environment:
  sdk: '>=2.17.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.0.0
  firebase_core: ^2.10.0
  firebase_auth: ^4.3.0
  go_router: ^13.2.0
  intl: ^0.18.1

flutter:
  uses-material-design: true

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^0.3.0
